# 物品信息编辑实施计划

1. 提取创建页现有的物品字段表单，保持字段、分类选择和样式不变。
   - 验证：创建页继续使用相同字段，TypeScript 与 lint 通过。
2. 在资产数据模块新增受 `AssetInput` 类型约束的更新方法。
   - 验证：只更新允许编辑的字段，并返回更新后的资产。
3. 新增独立编辑页，并从详情页右上角进入。
   - 验证：表单预填当前信息，空名称/搜索词不能提交，取消不会写入数据。
4. 保存后自动估价并刷新详情、历史和列表缓存。
   - 验证：估价成功记录新价格；估价失败保留编辑结果并显示可恢复提示。
5. 运行定向静态检查并审核最终 diff。
   - 验证：前端 lint 和 TypeScript 检查通过，变更只覆盖已批准功能。
